import { DynamicTodoDirective } from './dynamic-todo.directive';

describe('DynamicTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTodoDirective();
    expect(directive).toBeTruthy();
  });
});
